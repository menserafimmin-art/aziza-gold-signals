<!DOCTYPE html>
<html lang="kg">
<head>
<meta charset="UTF-8">
<title>Aziza Gold Signals</title>

<style>
body {
    margin: 0;
    background: #06111f; /* тёмно-синий фон */
    font-family: Arial, sans-serif;
    color: #ffffff;
}

/* HEADER */
.header {
    padding: 22px;
    text-align: center;
    background: linear-gradient(90deg, #0a2a47, #0f3a5c);
    border-bottom: 3px solid #ff7b00;
    font-size: 30px;
    font-weight: bold;
    letter-spacing: 1px;
    color: #ff7b00;
    text-shadow: 0px 0px 8px rgba(255,123,0,0.7);
}

/* MAIN */
.main {
    padding: 30px;
    text-align: center;
}

.main h1 {
    font-size: 26px;
    color: #4db4ff;
    margin-bottom: 15px;
}

.main p {
    font-size: 18px;
    line-height: 1.7;
    color: #dce7f1;
}

/* TIMER BLOCK */
.timer-box {
    background: #0f3a5c;
    border: 2px solid #ff7b00;
    padding: 22px;
    border-radius: 12px;
    margin: 30px auto;
    width: 300px;
    text-align: center;
}

#timer {
    font-size: 36px;
    font-weight: bold;
    color: #ff7b00;
    text-shadow: 0px 0px 6px rgba(255,123,0,0.7);
}

/* MAIN BUTTON */
.main button {
    background: #ff7b00;
    color: white;
    border: none;
    padding: 14px 24px;
    font-size: 20px;
    border-radius: 10px;
    cursor: pointer;
    transition: 0.2s;
}

.main button:hover {
    background: #ff8d1a;
    transform: scale(1.05);
}

/* POPUP */
.overlay {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(0,0,0,0.70);
    display: flex;
    justify-content: center;
    align-items: center;
}

.popup {
    background: #0f3a5c;
    padding: 28px;
    border-radius: 14px;
    max-width: 380px;
    text-align: center;
    border: 2px solid #ff7b00;
    box-shadow: 0px 0px 25px rgba(255,123,0,0.4);
}

.popup h2 {
    font-size: 26px;
    color: #ff7b00;
    margin-bottom: 15px;
}

.popup p {
    font-size: 18px;
    line-height: 1.5;
}

.popup button {
    background: #ff7b00;
    color: #fff;
    border: none;
    padding: 12px 20px;
    font-size: 18px;
    border-radius: 8px;
    cursor: pointer;
    margin-top: 10px;
}

.popup button:hover {
    background: #ff8d1a;
}

/* HIDE POPUP */
.hidden {
    display: none;
}

</style>
</head>
<body>

<!-- HEADER -->
<div class="header">
    Aziza Gold Signals
</div>

<!-- MAIN CONTENT -->
<div class="main">
    <h1>Премиум сигналдар</h1>
    <p>
        Сизге бот 20 мүнөттүн ичинде 100Х ге чейинки <b>5 сигнал</b> берет!<br><br>
        Бул мүмкүнчүлүктү колдонуп калуу үчүн 
        <b>баларыңызга 980 сом</b> толуктаныз!<br><br>
        Утушу ошол замат чечсеңиз болот 💸
    </p>

    <div class="timer-box">
        <p>Сигналдарга чейин:</p>
        <div id="timer">20:00</div>
    </div>

    <button onclick="window.location.href='https://t.me/Azizacash'">
        Активдештирүү
    </button>
</div>

<!-- POPUP -->
<div class="overlay" id="popup">
    <div class="popup">
        <h2>Куттуктайм! 🎉</h2>
        <p>
            Сизге бот 20 мүнөттүн ичинде 100Х ге чейинки <b>5 сигнал</b> берет!<br><br>
            Бул мүмкүнчүлүктү колдонуп калуу үчүн 
            <b>балансыңызга 980 сом</b> толуктаныз!<br><br>
            Утушу ошол замат чечсеңиз болот 💸
        </p>

function closePopup() 
{
    const popup = document.getElementById("popup");
    popup.style.display = "none"; 
}
        <button onclick="closePopup()">
            Түшүндүм
        </button>
    </div>
</div>


<script>
/* CLOSE POPUP */
function closePopup() {
    document.getElementById("popup").classList.add("hidden");
}

/* 20-MINUTE TIMER */
let time = 20 * 60;

function updateTimer() {
    let minutes = Math.floor(time / 60);
    let seconds = time % 60;

    document.getElementById("timer").innerHTML =
        (minutes < 10 ? "0" : "") + minutes + ":" +
        (seconds < 10 ? "0" : "") + seconds;

    if (time > 0) time--;
}

setInterval(updateTimer, 1000);
</script>

</body>
</html>
